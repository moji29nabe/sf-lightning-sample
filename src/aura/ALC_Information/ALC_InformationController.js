({
    editWeatherStatus: function(cmp, event, helper) {
        cmp.set("v.showWeatherDialog", true);
    },
    editTodoStatus: function(cmp, event, helper) {
        cmp.set("v.showTodoDialog", true);
    },
})